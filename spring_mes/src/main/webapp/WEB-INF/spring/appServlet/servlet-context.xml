<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- DispatcherServlet Context -->
	<mvc:annotation-driven />

	<!-- 🖼️ 정적 리소스 매핑 -->
	<mvc:resources mapping="/resources/**" location="/resources/" />

	<!-- ✅ 실제 이미지 경로 매핑 (C:/mes_3/upload/) -->
	<mvc:resources mapping="/upload/**" location="file:///C:/mes_3/upload/" />
	<mvc:resources mapping="/human4/upload/**" location="file:///C:/mes_3/upload/" />

	<!-- JSP View Resolver -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- 컴포넌트 스캔 -->
	<context:component-scan base-package="kr.or.mes2" />

	<!-- ✅ 인터셉터 설정 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<mvc:exclude-mapping path="/login" />
			<mvc:exclude-mapping path="/logout" />
			<mvc:exclude-mapping path="/mypage" />
			<mvc:exclude-mapping path="/password/reset" />
			<mvc:exclude-mapping path="/resources/**" />
			<mvc:exclude-mapping path="/static/**" />
			<beans:bean class="kr.or.mes2.interceptor.ForceChangePasswordInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- ✅ 파일 업로드 지원 -->
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.support.StandardServletMultipartResolver" />
</beans:beans>
